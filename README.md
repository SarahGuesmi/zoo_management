# zoo_management
This project was developed progressively through different "prosit" steps .

-Prosit :  
  Created the main class `ZooManagement`, declared attributes ('nbrCages', 'zooName').

Prosit 2:  
  Introduced the classes `Zoo` and `Animal` with their attributes. Implemented parameterized constructors, created objects ('lyon', 'myZoo'), and added methods such as 'displayZoo()'.  
  Also learned how to correctly display objects and override the 'toString()' method for better representation.
